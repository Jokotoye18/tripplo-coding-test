import React from 'react';
import {View} from 'react-native';

import {SafeAreaView, Text} from '@components';

export const TeamDetails = () => {
  return (
    <SafeAreaView>
      <View>
        <Text text="TeamDetails" />
      </View>
    </SafeAreaView>
  );
};
